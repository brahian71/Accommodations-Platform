{"ast":null,"code":"import { Observable, BehaviorSubject, combineLatest } from 'rxjs';\nimport { map, debounceTime, distinctUntilChanged } from 'rxjs/operators';\nimport { DEFAULT_FILTERS } from '../data/mock-properties';\nimport * as i0 from \"@angular/core\";\nimport * as i1 from \"./property.service\";\nexport let SearchService = /*#__PURE__*/(() => {\n  class SearchService {\n    constructor(propertyService) {\n      this.propertyService = propertyService;\n      this.searchParamsSubject = new BehaviorSubject({\n        destination: '',\n        checkIn: '',\n        checkOut: '',\n        guests: 1\n      });\n      this.filtersSubject = new BehaviorSubject(DEFAULT_FILTERS);\n      this.configSubject = new BehaviorSubject({\n        viewMode: 'list',\n        sortBy: 'relevance',\n        itemsPerPage: 6,\n        currentPage: 1\n      });\n      this.searchParams$ = this.searchParamsSubject.asObservable();\n      this.filters$ = this.filtersSubject.asObservable();\n      this.config$ = this.configSubject.asObservable();\n    }\n    /**\n     * Realizar búsqueda de propiedades con parámetros\n     */\n    searchProperties(params) {\n      this.updateSearchParams(params);\n      return this.propertyService.getProperties().pipe(map(properties => this.filterBySearchParams(properties, params)));\n    }\n    /**\n     * Aplicar filtros a las propiedades\n     */\n    applyFilters(properties, filters) {\n      return new Observable(observer => {\n        const filtered = properties.filter(property => {\n          // Filtro por tipo de propiedad\n          const propertyTypeMatch = !this.hasActivePropertyTypeFilters(filters) || filters.propertyTypes[property.propertyType];\n          // Filtro por experiencias\n          const experienceMatch = !this.hasActiveExperienceFilters(filters) || property.experiences.some(exp => filters.experiences[exp]);\n          // Filtro por amenidades\n          const amenityMatch = !this.hasActiveAmenityFilters(filters) || property.amenities.some(amenity => filters.amenities[amenity]);\n          // Filtro por rating\n          const ratingMatch = property.rating >= filters.minRating;\n          // Filtro por precio\n          const priceMatch = property.pricePerNight >= filters.priceMin && property.pricePerNight <= filters.priceMax;\n          return propertyTypeMatch && experienceMatch && amenityMatch && ratingMatch && priceMatch;\n        });\n        observer.next(filtered);\n        observer.complete();\n      });\n    }\n    /**\n     * Obtener resultados filtrados y ordenados\n     */\n    getFilteredResults() {\n      return combineLatest([this.propertyService.getProperties(), this.searchParams$, this.filters$, this.config$]).pipe(debounceTime(300), distinctUntilChanged(), map(([properties, searchParams, filters, config]) => {\n        // 1. Filtrar por parámetros de búsqueda\n        let filtered = this.filterBySearchParams(properties, searchParams);\n        // 2. Aplicar filtros\n        filtered = this.applyFiltersSync(filtered, filters);\n        // 3. Ordenar resultados\n        filtered = this.sortProperties(filtered, config.sortBy);\n        return filtered;\n      }));\n    }\n    /**\n     * Ordenar propiedades según criterio\n     */\n    sortProperties(properties, sortBy) {\n      const sorted = [...properties];\n      switch (sortBy) {\n        case 'price-asc':\n          return sorted.sort((a, b) => a.pricePerNight - b.pricePerNight);\n        case 'price-desc':\n          return sorted.sort((a, b) => b.pricePerNight - a.pricePerNight);\n        case 'rating':\n          return sorted.sort((a, b) => b.rating - a.rating);\n        case 'reviews':\n          return sorted.sort((a, b) => b.reviewsCount - a.reviewsCount);\n        case 'newest':\n          // En una app real, ordenaríamos por fecha de creación\n          return sorted.sort((a, b) => parseInt(b.id) - parseInt(a.id));\n        case 'relevance':\n        default:\n          // Ordenar por una combinación de rating y número de reseñas\n          return sorted.sort((a, b) => {\n            const scoreA = a.rating * Math.log(a.reviewsCount + 1);\n            const scoreB = b.rating * Math.log(b.reviewsCount + 1);\n            return scoreB - scoreA;\n          });\n      }\n    }\n    /**\n     * Actualizar parámetros de búsqueda\n     */\n    updateSearchParams(params) {\n      const current = this.searchParamsSubject.value;\n      this.searchParamsSubject.next({\n        ...current,\n        ...params\n      });\n    }\n    /**\n     * Actualizar filtros\n     */\n    updateFilters(filters) {\n      const current = this.filtersSubject.value;\n      this.filtersSubject.next({\n        ...current,\n        ...filters\n      });\n    }\n    /**\n     * Limpiar todos los filtros\n     */\n    clearFilters() {\n      this.filtersSubject.next(DEFAULT_FILTERS);\n    }\n    /**\n     * Actualizar configuración de resultados\n     */\n    updateConfig(config) {\n      const current = this.configSubject.value;\n      this.configSubject.next({\n        ...current,\n        ...config\n      });\n    }\n    /**\n     * Cambiar modo de vista\n     */\n    setViewMode(mode) {\n      this.updateConfig({\n        viewMode: mode\n      });\n    }\n    /**\n     * Cambiar criterio de ordenamiento\n     */\n    setSortBy(sortBy) {\n      this.updateConfig({\n        sortBy\n      });\n    }\n    /**\n     * Cambiar página actual\n     */\n    setCurrentPage(page) {\n      this.updateConfig({\n        currentPage: page\n      });\n    }\n    /**\n     * Obtener estado actual de filtros\n     */\n    getFilterState() {\n      return this.filters$.pipe(map(filters => ({\n        activeFilters: filters,\n        hasActiveFilters: this.hasAnyActiveFilters(filters)\n      })));\n    }\n    /**\n     * Búsqueda por texto libre\n     */\n    searchByText(searchTerm) {\n      return this.propertyService.searchProperties(searchTerm);\n    }\n    /**\n     * Obtener sugerencias de destinos\n     */\n    getDestinationSuggestions() {\n      return ['Salento', 'Valle de Cocora', 'Filandia', 'Armenia', 'Calarcá', 'Montenegro', 'Quimbaya', 'Circasia', 'La Tebaida', 'Buenavista'];\n    }\n    /**\n     * Validar fechas de búsqueda\n     */\n    validateSearchDates(checkIn, checkOut) {\n      if (!checkIn || !checkOut) {\n        return {\n          valid: false,\n          error: 'Fechas de llegada y salida son requeridas'\n        };\n      }\n      const checkInDate = new Date(checkIn);\n      const checkOutDate = new Date(checkOut);\n      const today = new Date();\n      today.setHours(0, 0, 0, 0);\n      if (checkInDate < today) {\n        return {\n          valid: false,\n          error: 'La fecha de llegada no puede ser en el pasado'\n        };\n      }\n      if (checkOutDate <= checkInDate) {\n        return {\n          valid: false,\n          error: 'La fecha de salida debe ser posterior a la llegada'\n        };\n      }\n      return {\n        valid: true\n      };\n    }\n    /**\n     * Calcular número de noches\n     */\n    calculateNights(checkIn, checkOut) {\n      if (!checkIn || !checkOut) return 0;\n      const checkInDate = new Date(checkIn);\n      const checkOutDate = new Date(checkOut);\n      const timeDiff = checkOutDate.getTime() - checkInDate.getTime();\n      return Math.ceil(timeDiff / (1000 * 3600 * 24));\n    }\n    // MÉTODOS PRIVADOS\n    filterBySearchParams(properties, params) {\n      let filtered = [...properties];\n      // Filtrar por destino\n      if (params.destination) {\n        const destination = params.destination.toLowerCase();\n        filtered = filtered.filter(property => property.shortLocation.toLowerCase().includes(destination) || property.location.toLowerCase().includes(destination));\n      }\n      // En una aplicación real, aquí filtrarías por disponibilidad de fechas\n      // if (params.checkIn && params.checkOut) {\n      //   filtered = this.filterByAvailability(filtered, params.checkIn, params.checkOut);\n      // }\n      // Filtrar por número de huéspedes (basado en características de la propiedad)\n      if (params.guests > 1) {\n        filtered = filtered.filter(property => {\n          // Lógica simple: asumimos capacidad basada en número de habitaciones\n          const bedrooms = this.extractBedroomCount(property.features);\n          return bedrooms * 2 >= params.guests; // 2 personas por habitación\n        });\n      }\n      return filtered;\n    }\n    applyFiltersSync(properties, filters) {\n      return properties.filter(property => {\n        const propertyTypeMatch = !this.hasActivePropertyTypeFilters(filters) || filters.propertyTypes[property.propertyType];\n        const experienceMatch = !this.hasActiveExperienceFilters(filters) || property.experiences.some(exp => filters.experiences[exp]);\n        const amenityMatch = !this.hasActiveAmenityFilters(filters) || property.amenities.some(amenity => filters.amenities[amenity]);\n        const ratingMatch = property.rating >= filters.minRating;\n        const priceMatch = property.pricePerNight >= filters.priceMin && property.pricePerNight <= filters.priceMax;\n        return propertyTypeMatch && experienceMatch && amenityMatch && ratingMatch && priceMatch;\n      });\n    }\n    hasActivePropertyTypeFilters(filters) {\n      return Object.values(filters.propertyTypes).some(filter => filter);\n    }\n    hasActiveExperienceFilters(filters) {\n      return Object.values(filters.experiences).some(filter => filter);\n    }\n    hasActiveAmenityFilters(filters) {\n      return Object.values(filters.amenities).some(filter => filter);\n    }\n    hasAnyActiveFilters(filters) {\n      return this.hasActivePropertyTypeFilters(filters) || this.hasActiveExperienceFilters(filters) || this.hasActiveAmenityFilters(filters) || filters.minRating > 0 || filters.priceMin > 0 || filters.priceMax < 500000;\n    }\n    extractBedroomCount(features) {\n      for (const feature of features) {\n        const match = feature.match(/(\\d+)\\s*habitacion/i);\n        if (match) {\n          return parseInt(match[1]);\n        }\n      }\n      return 1; // Default si no se encuentra información\n    }\n    static {\n      this.ɵfac = function SearchService_Factory(t) {\n        return new (t || SearchService)(i0.ɵɵinject(i1.PropertyService));\n      };\n    }\n    static {\n      this.ɵprov = /*@__PURE__*/i0.ɵɵdefineInjectable({\n        token: SearchService,\n        factory: SearchService.ɵfac,\n        providedIn: 'root'\n      });\n    }\n  }\n  return SearchService;\n})();","map":null,"metadata":{},"sourceType":"module","externalDependencies":[]}